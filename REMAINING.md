# Limo Audit — Remaining Work (89 items not auto-fixed)

Need a refactor / design decision / new helper or deployer / dependency bump, or judged too risky for a mechanical edit. Full detail in AUDIT_FINDINGS.md; all 261 with exact fixes in FIXES.md.

## High (5)
- **F002** `src/core/lspakfilelistentry.h:17` [Security] — Out-of-bounds read: non-NUL-terminated path[256] from untrusted .pak read as C-string
  - **Fix:** Treat the field as bounded: construct the path/string with an explicit length, e.g. std::string(entry.path, strnlen(entry.path, sizeof(entry.path))), and reject entries whose 256 bytes contain no terminator. Do this where LsPakFileListEntry is materialized in getFileList().
  - _Agent note:_ The finding's own fix targets getFileList() where LsPakFileListEntry is materialized — that code lives in a different .cpp file, not this header. This header only defines the packed struct, whose char path[256] must mirror the on-disk .pak binary layout byte-for-byte; altering it would break parsing. No localized, compiling fix exists within this single header file, so the bounded-string construction must be done at the materialization site (out of scope for this file).
- **F003** `src/core/moddedapplication.cpp:1543` [Security] — Imported instance bundle can inject arbitrary shell commands via deploy hooks (std::system)
  - **Fix:** Do not auto-load executable hooks from imported bundles. Either strip the "hooks" (and tool command) fields during importInstanceInto/parseInstanceBundle, or require explicit user review/confirmation before any imported hook is persisted or run. Prefer running hooks via an argv exec (posix_spawn/execvp) rather than std::system, and surface the exact command to the user before first execution.
- **F005** `src/core/tool.cpp:254,262` [Security] — Arguments and Protontricks-arguments fields are appended to the shell command unescaped
  - **Fix:** Either build and exec the argument list via execvp-style argv rather than a shell string, or tokenize and shellEscape each argument. At minimum document and validate that these fields are shell-interpreted.
  - _Agent note:_ The robust fix requires switching from a popen shell string to an execvp-style argv list, or shell-tokenizing arguments_/protontricks_arguments_ — a non-localized refactor that risks breaking legitimate shell features users put in these fields (flags, redirects, multiple args). The remaining 'document/validate' suggestion is not a code fix. Skipped to avoid a risky change.
- **F007** `src/ui/mainwindow.cpp:1613-1633 (runCommand), 1635-1655 (runConcurrent), callers 3285/4946/5339` [Security] — runCommand passes unescaped command strings straight to popen()/shell
  - **Fix:** Avoid the shell entirely: use QProcess with an argv list (program + arguments) instead of popen on a shell string. If a shell is unavoidable, run via execvp-style argv or rigorously quote each argument; single-quote-only wrapping is insufficient because embedded single quotes break out. Treat imported tool/app command strings as untrusted and validate/escape them.
- **F008** `src/ui/nexusmoddialog.cpp:38-39, 152-155, 114-119, 146-151` [Security] — Untrusted NexusMods strings rendered as HTML without escaping (markup injection)
  - **Fix:** HTML-escape every plain-text field (name, version, category_name, the put_time string, external_virus_scan_url before using it as link text) with QString::toHtmlEscaped() before inserting into RichText, and make bbcodeToHtml() escape <, >, & in the raw input before applying token substitutions so embedded raw HTML in descriptions cannot survive.
  - _Agent note:_ Requires broad multi-field escaping plus reworking bbcodeToHtml to pre-escape and validate schemes; not localized and risks double-escaping/breaking rendering.

## Medium (36)
- **F088** `CMakeLists.txt:519-520` [Code Quality] — configure_file writes generated consts.h into the source tree and it is committed to git
  - **Fix:** Generate into ${CMAKE_CURRENT_BINARY_DIR}/src/core/consts.h, add that build include dir to the target's include path, remove src/core/consts.h from version control, and gitignore it. Keep only consts.h.in tracked.
  - _Agent note:_ Not a localized single-file change. Moving the generated consts.h to ${CMAKE_CURRENT_BINARY_DIR}/src/core requires removing the committed src/core/consts.h from version control and editing .gitignore (other files), plus adding the build include dir. With the source-tree copy still present and ${PROJECT_SOURCE_DIR}/src on the include path, the stale committed header would shadow the generated one, risking a broken/inconsistent build. Per the SKIP rule (refactor touching other files + build risk), left unchanged.
- **F089** `src/core/tagconditionnode.cpp:110` [Code Quality] — Regex recompiled for every file instead of once per condition
  - **Fix:** Compile the std::regex once when the node is built (or memoize it) and reuse it in the loop.
  - _Agent note:_ Memoizing/compiling the std::regex once requires adding a member field to the class (header change) or a static cache; cannot be done within this single .cpp file without modifying the header or risking compile. Same root cause as F020. Skipped.
- **F086** `src/core/remote/modio_provider.cpp:203-229` [Feature Gap] — mod.io getDownloadUrl is a partial stub; authenticated/subscriber downloads unimplemented
  - **Fix:** Implement the documented mod.io OAuth token flow (or the modfile download endpoint with Authorization: Bearer) and surface a clear, user-facing message when a mod requires authentication that is not configured.
- **F087** `src/core/remote/remotesourceregistry.h:41-117` [Feature Gap] — RemoteSourceRegistry and Thunderstore/GameBanana/mod.io providers are unwired dead scaffold
  - **Fix:** Either wire the registry into the repositories/import UI (provider selection -> search -> install) or clearly mark the providers as experimental/disabled. Add at least smoke tests so the parse paths are exercised.
- **F031** `src/core/cryptography.cpp:70` [Functionality] — Corrupt/short installation key file is silently regenerated, making stored API keys undecryptable
  - **Fix:** Treat a wrong-size key file as an error (surface it to the user) rather than silently regenerating, or back up the old file before regenerating and warn that stored keys must be re-entered.
  - _Agent note:_ Skipped: not a localized fix. Treating a wrong-size key file as a user-surfaced error vs. backing it up and warning is a design/UX decision that changes behavior (new file ops, error propagation up the call stack, user messaging) and risks altering the existing fall-through-to-regenerate logic. Out of scope for a surgical edit.
- **F035** `src/core/lspakextractor.cpp:82` [Functionality] — Decompression output buffers sized from untrusted uncompressed_size with no global cap on file-list path
  - **Fix:** Lower the per-file cap to a realistic meta.lsx size (these are small XML files, e.g. a few MB), and bound total allocation across the file list; reject archives whose summed declared sizes are implausible.
  - _Agent note:_ extractData is shared between individual-file extraction and the file-list extraction (where uncompressed_list_size can legitimately reach ~280MB). Lowering the per-file cap risks rejecting valid archives, and bounding cumulative allocation across the file list requires new accumulator state/refactoring across calls. Not a localized change; risk of breaking valid extraction, so skipped.
- **F039** `src/core/moddedapplication.cpp:741` [Functionality] — Deployer index parameters are never bounds-checked before indexing deployers_
  - **Fix:** Add a consistent bounds check (deployer >= 0 && deployer < deployers_.size()) at the top of each deployer-indexed public method, returning/throwing a controlled error like the profile/tool/backup methods already do.
  - _Agent note:_ getLoadorder returns shared_ptr; adding a guard would return nullptr and risk downstream deref, not obviously safe
- **F040** `src/core/moddedapplication.cpp:751` [Functionality] — setStagingDir moves mods/config with sfs::rename and no error handling, can lose data across filesystems
  - **Fix:** Use the std::error_code overloads, detect cross-device failure and fall back to copy+remove, and perform the move transactionally (or at least roll back already-moved entries) before committing staging_dir_.
  - _Agent note:_ needs error_code overloads + cross-device copy/remove fallback + rollback, a refactor
- **F042** `src/core/moddedapplication.cpp:1166` [Functionality] — removeModFromGroup can erase active_group_members_ out of sync, risking later out-of-bounds in updateDeployerGroups
  - **Fix:** Keep all per-group parallel vectors (groups_, active_group_members_, group_names_, group_notes_) strictly in lockstep with a single helper that erases a group from all of them, and assert/bounds-check sizes after load.
- **F044** `src/core/moddedapplication.cpp:2261-2288` [Functionality] — exportProfile leaves deployers on the wrong profile if serialization throws (no RAII restore)
  - **Fix:** Use an RAII guard that restores the deployer's previous profile in its destructor, so any exception during serialization restores state. Apply the same pattern to the other transient setProfile loops.
  - _Agent note:_ requires an RAII restore-guard helper; exception-safety refactor, not localized
- **F051** `src/core/nexus/api.cpp:559-566` [Functionality] — extractDomainAndModId uses std::stoi (throws / truncates) and returns int for a long mod id
  - **Fix:** Parse with std::stoll into a long and wrap in try/catch (or std::from_chars) returning std::nullopt on failure; change the optional's second type to long to match getMod(long).
  - _Agent note:_ try/catch already guards stoi; changing optional<int> to long requires editing the header signature (out of scope)
- **F053** `src/core/openmwplugindeployer.cpp:52-66` [Functionality] — OpenMW unDeploy backup is never restored due to single-file backup vs. LootDeployer two-file restore
  - **Fix:** Override restoreUndeployBackupIfExists in OpenMwPluginDeployer to use the single-file PluginDeployer semantics, or have unDeploy also back up the loadorder file expected by LootDeployer's restore logic.
  - _Agent note:_ Requires overriding restoreUndeployBackupIfExists / two-file backup semantics, a base-class design refactor, not a localized fix.
- **F054** `src/core/openmwplugindeployer.cpp:646-726` [Functionality] — Data= block parser corrupts a user's BEGIN-without-END marker and loses cfg content
  - **Fix:** If EOF is reached while in_block is still true, treat the unterminated block as not-Limo-owned (re-include the buffered lines), or detect/log the malformed block and abort the rewrite rather than dropping content.
- **F055** `src/core/overlaydeployer.cpp:288-306` [Functionality] — orig/ snapshot is created only when empty and never refreshed; copy may capture already-mounted overlay content
  - **Fix:** Verify dest_path_ is definitively not a mount point (re-check isMounted via /proc/mounts) immediately before snapshotting, and provide a way to refresh orig/ (or detect game-file changes) rather than freezing it on first deploy.
  - _Agent note:_ Skipped: the fix requires a design decision (a mechanism to refresh/invalidate the orig/ snapshot or detect game-file changes), not a localized guard. Adding only an isMounted() re-check before snapshotting would not address the core concern (snapshot frozen on first deploy) and risks changing deploy semantics; doUnmount() already runs before this point. Left unchanged to avoid a partial/incorrect fix.
- **F058** `src/core/remote/ommrepository.cpp:258-279` [Functionality] — Downloaded files are never integrity-verified despite parsed size/checksum
  - **Fix:** Plumb the advertised size and checksum through RemoteDownloadInfo/ImportModInfo and verify them after download (reuse the existing integrity-verifier hashing). At minimum verify the byte count matches the advertised size and log a warning when a checksum is present but unverifiable.
  - _Agent note:_ Integrity verification requires plumbing the advertised size/checksum through RemoteDownloadInfo/ImportModInfo and reusing the existing hashing/integrity-verifier in the download/import flow, which spans multiple other files and is a cross-component refactor. Out of scope for a localized single-file edit.
- **F059** `src/core/reversedeployer.cpp:51` [Functionality] — updateManagedFiles sets progress total from stale count before recomputing it, breaking progress
  - **Fix:** Compute the file count first (or count files in dest_path_ up front) and call setTotalSteps with the actual number before iterating, rather than the previous run's cached value.
  - _Agent note:_ Skipped: fixing the stale progress total requires pre-counting files in dest_path_ before iterating, but updateFilesInDir both counts and mutates managed state in one pass. A correct fix needs either a separate counting pass or a refactor of updateFilesInDir, which is a design decision, not a localized edit. This is a progress-bar accuracy issue, not a correctness bug.
- **F060** `src/core/reversedeployer.cpp:644` [Functionality] — readManagedFiles trusts JSON-derived file paths used directly in filesystem remove/link sinks
  - **Fix:** On load, reject or sanitize entries whose lexically_normal path escapes the base (contains '..' or is absolute), and verify dest_path_/path stays within dest_path_ before any remove/link/copy.
  - _Agent note:_ Skipped: sanitizing JSON-derived paths against base escape requires adding lexically_normal/containment validation across multiple filesystem sinks (remove/link/copy) in readManagedFiles and elsewhere. This is a non-trivial security refactor, not a single localized guard, and getting path-containment semantics correct without regressions or compile risk is not safe to do surgically here.
- **F063** `src/ui/addappdialog.cpp:477-520` [Functionality] — Deploy hooks silently discarded when the app config file does not yet exist
  - **Fix:** Surface a warning to the user when hooks could not be persisted, or route hooks through EditApplicationInfo/setDeployHooks so they are saved atomically with the rest of the edit rather than via a best-effort side-write.
  - _Agent note:_ Fix requires routing hooks through EditApplicationInfo/setDeployHooks (a public-API/struct change in an out-of-scope header, per the existing code comment) or adding new warning UI plumbing. Not a localized one-file change; skipped to avoid breaking the build.
- **F066** `src/ui/applicationmanager.cpp:1454-1480` [Functionality] — Download retry/queue item discards version_overwrite and target_group_id on completion
  - **Fix:** Validate target_group_id against the app's current groups when consuming a restored queue item, and clear it (set -1) if the group no longer exists before emitting downloadComplete.
  - _Agent note:_ requires validating/clearing target_group_id against current groups; non-localized design change
- **F067** `src/ui/applicationmanager.cpp:1531-1541` [Functionality] — Download queue file path changes after the first app is added, orphaning a previously saved queue
  - **Fix:** Use a single stable location for the queue file (e.g. always the user AppDataLocation) independent of which apps exist or their ordering, so the persisted queue is found consistently across restarts.
  - _Agent note:_ stable queue location is a design change to getDownloadQueuePath behavior; risky
- **F068** `src/ui/deployerlistmodel.cpp:64-65, 74-79` [Functionality] — data() reinterprets every node as DeployerModInfo, including separator/root DeployerEntry objects
  - **Fix:** Only form the DeployerModInfo alias after confirming !data->isSeparator (dynamic_cast or the isSeparator flag), and never read derived members for separator/root nodes. Consider storing a discriminated type rather than relying on reinterpret/static aliasing.
  - _Agent note:_ Requires refactor/design change. The suggested fix is to store a discriminated type instead of reinterpret/static aliasing of TreeItem<DeployerEntry> as TreeItem<DeployerModInfo>. In practice the genuinely-derived members (sourceName, enabled, auto_tags, manual_tags) are already guarded by !data->isSeparator checks (lines 69, 88, 106, 114, 129) before being read; only the base-class member 'id' (shared at the same offset for both classes) is read unguarded in the ForegroundRole branch, which is memory-safe. A broad restructuring to defer alias formation across the whole data()/setData() flow is not localized and risks the build, so skipped per the when-in-doubt rule.
- **F070** `src/ui/deployerlistproxymodel.cpp:16-28` [Functionality] — Proxy data() ForegroundRole indexes row_text_colors_ by proxy row that may be stale after filtering
  - **Fix:** Key the colour by mod id (compute conflict colour on demand from conflict_groups_) instead of caching by proxy row, or invalidate/rebuild row_text_colors_ on every filter/layout change before the view repaints.
  - _Agent note:_ Requires a refactor: keying conflict colours by mod id computed on demand from conflict_groups_, or restructuring when/how row_text_colors_ is rebuilt across filter/layout changes. This is a design change affecting the data()/updateFilter() flow, not a localized guard, so skipped to avoid breaking the existing repaint behavior. (A bounds guard for row >= row_text_colors_.size() already exists at line 22.)
- **F071** `src/ui/edittoolwidget.cpp:185-231` [Functionality] — Steam tool app id parsed with toInt() silently yields 0 on overflow/empty
  - **Fix:** Use toLongLong with the bool* ok flag, validate the result fits the steam_app_id_ type, and require a non-empty app id (set VALID_NOT_EMPTY / a min-length validator) when the Steam or Protontricks runtime is selected.
  - _Agent note:_ Not a localized fix. Tool's constructor and steam_app_id_ member are typed int (src/core/tool.h), so the suggested toLongLong+ok-flag change would require narrowing back to int plus overflow validation, and the rest of the fix (conditionally swapping the field validator to VALID_NOT_EMPTY / a min-length validator based on the selected runtime) is a validation/design change that risks breaking the build and existing behavior. The app_id_field_ already has a [0-9]* regex validator. Per the SKIP-when-in-doubt rule, left unchanged.
- **F073** `src/ui/mainwindow.cpp:173-182` [Functionality] — Worker thread forcibly terminated on exit can corrupt mod state
  - **Fix:** Before destroying, signal the worker to stop accepting new work, disable the busy/long operations, and wait without a hard timeout (or a much larger one) for in-flight filesystem writes to finish. Avoid terminate() entirely for a worker that writes to disk; instead make long operations cancellable and join cleanly. Delete app_manager_ via deleteLater() on the worker thread (or only after wait() confirms the thread finished), never with a raw delete from the GUI thread after terminate().
  - _Agent note:_ Requires worker-thread cancellation/join redesign and deleteLater plumbing, not a localized fix.
- **F076** `src/ui/mainwindow.cpp:1998-2015` [Functionality] — versionIsLessOrEqual mishandles differing-length versions and non-numeric input
  - **Fix:** Iterate over the max length, treating missing/empty components as 0, compare component-by-component returning on the first inequality, and decide an explicit policy for non-numeric input rather than defaulting to true.
  - _Agent note:_ Differing-length/non-numeric version comparison needs a semantic rewrite; too risky to apply safely.
- **F080** `src/ui/repositoriesdialog.cpp:155-158, 217-219, 256-258` [Functionality] — Repository network calls (connect/listPackages/resolveDownloadUrl) run synchronously on the UI thread
  - **Fix:** Move the repository network operations off the UI thread (QtConcurrent + QFutureWatcher as in nexusbrowserdialog), show a busy/loading state, and allow cancellation.
- **F082** `src/ui/savemanagerwidget.cpp:165-198` [Functionality] — Save deletion maps selected rows via stale UserRole index, can delete the wrong file after sorting
  - **Fix:** Resolve the save by a stable key (e.g. store the absolute path in UserRole and delete by path), and list the actual file name(s) in the confirmation dialog so the user can verify before a destructive, non-undoable delete.
  - _Agent note:_ QTableWidget sorting moves items with their UserRole intact and saves_ is not reordered, so the index is not stale; the proposed path-key/dialog change is a behavior-changing refactor.
- **F083** `tests/CMakeLists.txt:34-43` [Functionality] — LOOT and OpenMW deployer tests are compiled out by default, leaving Bethesda/Morrowind load-order logic untested in normal builds
  - **Fix:** Either provide a CI build matrix entry with LIMO_WITH_LOOT=ON so these tests actually run, or factor the pure parsing/load-order logic out from libloot so it can be unit-tested with LOOT OFF. At minimum, document that these tests are dark in the default configuration.
- **F084** `tests/test_utils.cpp:8-37` [Functionality] — verifyDirsAreEqual does not compare file contents unless test_content=true; many deploy tests assert only the set of paths
  - **Fix:** Pass test_content=true wherever the winning content matters (especially the symlink and case-matching deploy tests), or add a dedicated content/symlink-target assertion. Resolve symlinks and compare the dereferenced bytes in the symlink test.
  - _Agent note:_ Requires editing other deploy test files / test-coverage design decision, not localized to this file.
- **F012** `.github/workflows/release.yml:113-118` [Security] — linuxdeploy downloaded from unpinned 'continuous' release with no checksum/signature verification
  - **Fix:** Pin to a specific linuxdeploy release tag (not 'continuous') and verify a SHA256 checksum before chmod/exec, e.g. download the matching .sha256 (or hardcode the expected hash) and run `sha256sum -c` and abort on mismatch. Optionally vendor a known-good copy.
- **F013** `src/core/cryptography.cpp:142-145` [Security] — Weak key derivation: master password byte-repeated to 32 bytes with no KDF or salt
  - **Fix:** Derive the AES key from the master password with a salted, memory-hard KDF (e.g. PBKDF2-HMAC-SHA256 with a per-file random salt and high iteration count, or Argon2id via libsodium). Store the salt alongside the ciphertext/nonce/tag. Reject or warn on empty/very short passwords.
- **F018** `src/core/overlaydeployer.cpp:345-356` [Security] — Shell command built with unescaped single-quote wrapping (command injection on '-containing paths)
  - **Fix:** Replace every '{}' wrapping of a path/binary in runCommand call sites with a proper POSIX single-quote escaper (reuse/extract tool.cpp's shellEscape so each embedded ' becomes '\''). Better yet, avoid the shell entirely: use posix_spawn/execvp with an argv array so no quoting is needed at all.
- **F020** `src/core/tagconditionnode.cpp:109-110` [Security] — Unvalidated user/config regex compiled per-file enables ReDoS hang and uncaught std::regex_error
  - **Fix:** Validate and compile the regex once in the TagConditionNode constructor (store a std::regex member) inside a try/catch, rejecting invalid patterns at load time with a clear error; consider a complexity/length cap or std::regex_constants to limit backtracking, and wrap evaluation in try/catch so a bad pattern degrades gracefully instead of crashing.
  - _Agent note:_ Proper fix requires storing a compiled std::regex member compiled once in the constructor inside try/catch, which means modifying the header (tagconditionnode.h) to add the member. Only this one .cpp file may be edited, so the per-file recompile and validate-at-load-time fix cannot be done here without a header/refactor change. Skipped to avoid scope/compile risk.
- **F022** `src/ui/addappdialog.cpp:283-301` [Security] — Deployer target_dir from community game config is created on disk without path validation
  - **Fix:** Validate resolved target/source paths before create_directories: reject absolute paths that escape the expected install/prefix roots, reject '..' components, and restrict directory creation to inside the staging/prefix/install dirs. Surface skipped deployers to the user.
  - _Agent note:_ Requires non-localized path-validation logic (escape-root checks, '..' rejection, restricting creation to staging/prefix/install roots) plus a new user-facing surfacing mechanism for skipped deployers. This is a security-design refactor, not a surgical edit; skipped to avoid build/behavior risk.
- **F024** `src/ui/nexusmoddialog.cpp:150` [Security] — Network-supplied virus-scan URL placed unescaped inside an href attribute
  - **Fix:** Escape the URL with toHtmlEscaped() and validate the scheme (allow only http/https) before building the anchor; do the same for BBCode-derived [url]/[img]/[youtube] hrefs.
  - _Agent note:_ URL escaping/scheme validation across href and BBCode tokens is non-localized and entangled with the bbcodeToHtml refactor; skipped per rules.
- **F025** `tests/test_installer.cpp:9-104` [Security] — No test exercises path-traversal / zip-slip protection or any untrusted-archive extraction path
  - **Fix:** Add fixtures: an archive whose entries include `../escape.txt`, an absolute path, and a symlink escaping the dest; assert that after Installer::extract/install nothing is written outside the destination. Add an OMOD fixture (incl. one with a malformed/oversized length and a `..` entry) to pin extractOmodArchive's traversal guard and size checks.
  - _Agent note:_ Requires authoring new test cases and binary malicious-archive/OMOD fixtures on disk, not a localized surgical edit.

## Low (48)
- **F214** `.github/workflows/ci.yml:42` [Code Quality] — cpr cache key omits compiler and system libcurl version, risking stale/ABI-mismatched cache
  - **Fix:** Add the compiler version and `dpkg -s libcurl4-openssl-dev`/runner image label to the cache key (e.g. include `${{ runner.os }}-${{ env.ImageOS }}` and a hash of relevant `dpkg --version` output).
  - _Agent note:_ The suggested cache-key change relies on `${{ env.ImageOS }}` (not defined/guaranteed in this workflow) and a hash of `dpkg --version` output, requiring a restructure of the cache step rather than a surgical edit. The existing key already pins the runner image (`ubuntu-24.04`) and the cpr ref, mitigating most ABI-mismatch risk. Treated as a design change per the when-in-doubt-skip rule.
- **F216** `flatpak/io.github.limo_app.limo.metainfo.xml:37-44` [Code Quality] — Metainfo/changelogs/version metadata is stale relative to the fork (still 1.2.2, upstream URLs)
  - **Fix:** Add release entries for the fork's versions to both metainfo.xml and changelogs.json, bump project VERSION, and update homepage/vcs/screenshot URLs to the fork if it is distributed independently.
  - _Agent note:_ Skipped: not a localized mechanical fix. This requires a design/distribution decision (whether the fork is distributed independently before changing the app-id-bound homepage/vcs/screenshot URLs away from upstream, which could break Flatpak/Flathub validation against the io.github.limo_app.limo id) plus information I do not have (the fork's actual release versions, dates, and changelog entries). Bumping project VERSION also lives in a different file (CMakeLists/version), which I am not permitted to edit. Inventing release entries with fabricated dates/notes would be incorrect, so per the when-in-doubt-skip rule I made no change.
- **F225** `src/core/lootdeployer.cpp:923-950` [Code Quality] — downloadList only percent-encodes spaces in URLs
  - **Fix:** Use a proper URL-encoding routine (e.g. cpr/curl escaping, or QUrl::toEncoded) for the path component instead of a single space->%20 replace, or document that callers must supply pre-encoded URLs.
  - _Agent note:_ Replacing the space->%20 replace with proper URL-path encoding (QUrl::toEncoded would pull in Qt headers not currently included; cpr/curl escaping over the whole URL would corrupt scheme/host) is a refactor / design decision with real risk of changing behavior or breaking the build. Skipped per when-in-doubt guidance.
- **F227** `src/core/moddedapplication.cpp:166` [Code Quality] — Duplicated, fragile mod-id allocation logic in installMod and createEmptyMod
  - **Fix:** Extract a single private allocateNewModId() helper used by both call sites, and make the id comparison explicit (compare m.id) rather than relying on Mod::operator<.
- **F228** `src/core/moddedapplication.cpp:2463-2489` [Code Quality] — Identical recursive load-order filter_node lambda duplicated between importProfile and restoreRestorePoint
  - **Fix:** Extract a single private helper (e.g. filterLoadorderForInstalledMods(const Json::Value&)) and call it from both importProfile and restoreRestorePoint.
- **F235** `src/core/progressnode.cpp:33-43` [Code Quality] — totalSteps()/setTotalSteps mismatch type (int vs uint64_t) and total_steps_ is uninitialized
  - **Fix:** Return uint64_t from totalSteps() and default-initialize total_steps_ = 0 in the class definition.
  - _Agent note:_ The fix requires editing the header (src/core/progressnode.h), which is out of scope. Both parts live there: the totalSteps() declaration is 'int totalSteps() const;' (line 51), so changing only the .cpp definition to return uint64_t would mismatch the declaration and break the build; and the uninitialized 'total_steps_' member (line 101) must be default-initialized to 0 in the header class definition. No safe, compiling .cpp-only change is possible, so skipped.
- **F236** `src/core/remote/thunderstoreprovider.cpp:48-49` [Code Quality] — Dead conditional: mod.name assigned the same value on both branches
  - **Fix:** Replace with `mod.name = pname;` or implement the intended distinct display-name logic. Remove the no-op conditional.
- **F240** `src/core/tw3scriptmerge.cpp:60-70` [Code Quality] — O(n*m) full LCS table can exhaust memory on a maliciously large script pair
  - **Fix:** Cap the line counts (skip merging and report a conflict when either file exceeds a sane threshold) or switch to the linear-space Hirschberg / Myers diff suggested in the TODO.
- **F243** `src/ui/addprofiledialog.cpp:16-46` [Code Quality] — Uninitialized int members app_id_ and profile_
  - **Fix:** Give the members in-class default initializers (e.g. 'int app_id_ = -1;') so a mis-sequenced call fails predictably.
  - _Agent note:_ The members app_id_ and profile_ are declared in the header file src/ui/addprofiledialog.h (lines 39 and 41), not in addprofiledialog.cpp. The described fix (in-class default initializers like 'int app_id_ = -1;') must be applied to the header, but I am restricted to editing only addprofiledialog.cpp. No localized fix exists within the .cpp file that matches the described change, so skipped to avoid touching another file.
- **F248** `src/ui/deployerlistview.cpp:132, 144` [Code Quality] — rowsAboutToBeRemoved called directly as a function instead of via beginRemoveRows/endRemoveRows
  - **Fix:** Perform structural moves through the model API using beginMoveRows/endMoveRows (or beginRemoveRows/endRemoveRows + begin/endInsertRows) inside the model, and have the view request the move rather than poking signal emitters directly.
- **F251** `src/ui/fomodcheckbox.cpp:22` [Code Quality] — FOMOD plugin image re-decoded from disk on every hover with no caching
  - **Fix:** Decode the QPixmap once (e.g. lazily on first hover and cache it as a member), and factor the shared hover/preview logic out of the two near-identical button classes.
- **F252** `src/ui/mainwindow.cpp:1572 and 1596` [Code Quality] — Operator-precedence-fragile log-visibility condition without parentheses
  - **Fix:** Add explicit parentheses: (*show_error && level <= LOG_ERROR) || (*show_warning && level <= LOG_WARNING), and factor the two near-identical printer lambdas into one helper to remove the duplication.
- **F255** `src/ui/modlistmodel.cpp:345-359` [Code Quality] — Size-string formatting has signed/unsigned comparison and fragile rounding
  - **Fix:** Use a size_t loop counter, clamp exp to units.size()-1, and replace the manual digit math with QLocale::formattedDataSize or a simple printf("%.1f %s")-style formatter.
  - _Agent note:_ exp is already std::size_t and the loop is clamped via exp+1<units.size(); replacing the rounding with QLocale::formattedDataSize is a non-localized refactor, so skipped.
- **F256** `src/ui/nexusmoddialog.cpp:52-53, 91-94` [Code Quality] — files_widget children deleted with delete in a loop instead of layout-safe teardown
  - **Fix:** Use a single helper that removes and deletes all child widgets via the layout (e.g. qDeleteAll of layout items) and recreates the layout once, removing the duplicated swap logic.
- **F258** `tests/test_deployer.cpp:284-292` [Code Quality] — Symlink assertion excludes files by filename (not path), so any file named 0/wasd/file.cfg anywhere escapes the check
  - **Fix:** Exclude by full relative path (the specific known non-overwritten files), not by bare filename, and add a comment explaining why each excluded entry is expected not to be a symlink.
  - _Agent note:_ Converting filename exclusions to full relative paths needs knowledge of target/mod012 layout; risky and could break the test.
- **F213** `src/ui/mainwindow.cpp:5206-5215` [Feature Gap] — Deploy preview (fork #49) confirm step is permanently dead code
  - **Fix:** Either complete the wiring (have ApplicationManager emit the computed plans and populate deploy_preview_plans_/show_deploy_preview_ from settings), or remove the dead branch so the deploy path is not misleading.
  - _Agent note:_ Completing/removing the deploy-preview seam is a feature/design decision out of scope here.
- **F110** `.github/workflows/release.yml:120-141` [Functionality] — AppImage build failure silently degrades the published Release to tarball-only
  - **Fix:** After the AppImage step, gate the Release publish on the AppImage having been produced (check steps.appimage.outcome / presence of the file) and fail or annotate the run when it is missing, so a broken AppImage does not produce a silent tarball-only release.
  - _Agent note:_ Target is a GitHub Actions YAML workflow file, not a C++/Qt6 source file, and the fix is a CI/CD design decision rather than a localized source edit. The continue-on-error:true on the AppImage step is intentional (documented in the file's header comment and the step comment) so the tarball fallback can still produce a usable artifact. Changing the release-gating semantics (failing the run or conditionally skipping the Release publish when the AppImage is absent) alters intended workflow behavior and could fail legitimate tarball-only releases; this is out of scope for a surgical source fix and risks breaking the release pipeline.
- **F119** `src/core/cryptography.cpp:246-256` [Functionality] — decrypt() with no master password generates an installation key as a side effect of a read-only operation
  - **Fix:** Only consult installationKey() during decryption if the key file already exists; otherwise skip straight to the default_key fallback, avoiding key generation as a side effect of a read.
  - _Agent note:_ Skipped: a clean fix requires checking whether the installation key file exists before calling installationKey() in decrypt(). The path-resolution logic lives in installationKeyDir() inside an anonymous namespace (not accessible from decrypt's scope), so guarding on existence would require duplicating that path logic, which risks divergence and is not a safe localized edit. Better handled by adding/exposing a helper, which is out of scope.
- **F120** `src/core/cyberpunkdeployer.cpp:95` [Functionality] — Archive load-order prefix breaks for loadorder indices > 9999 (4-digit field) silently
  - **Fix:** Either compute the prefix width from loadorder.size() (digits = number of digits in size-1) or clamp/validate and log when the load order exceeds the fixed width, so ordering correctness is not silently violated.
  - _Agent note:_ Skipped: applying the suggested fix requires passing loadorder.size() into destinationPath() to compute the prefix width (or to log when exceeded), which means a signature change plus changes in the getDeploymentMaps caller, i.e. not a localized edit. The existing TODO at line 92-93 already documents the fixed 4-digit width as a deliberate decision (>9999 mods is beyond any realistic Cyberpunk setup).
- **F125** `src/core/deployer.cpp:662-672` [Functionality] — Deploy step removes target then recreates link non-atomically — crash window can leave file missing
  - **Fix:** Create the new link/copy under a temporary name in the same directory and sfs::rename it over dest_path (atomic on a single filesystem) instead of remove-then-create, so an interrupted deploy never leaves the target absent.
  - _Agent note:_ Requires reworking the deploy step to create the link/copy under a unique temporary name then sfs::rename it over dest_path, including temp-file cleanup on the existing cross-device-link catch path. This is a non-localized refactor with real risk of leaving temp files or breaking the existing error handling, so per the when-in-doubt-skip rule it was not applied.
- **F126** `src/core/deployer.cpp:1207-1245` [Functionality] — keepOrRevertFileModifications catches std::runtime_error but filesystem ops throw std::filesystem::filesystem_error subclass — works, but the recovery path can itself throw uncaught
  - **Fix:** Guard the recovery filesystem calls and, on failure, restore the previously removed mod_file_path (or stage the work so the source file is only removed after the replacement is safely in place). Use std::error_code overloads where a failure should not be fatal.
  - _Agent note:_ The requested fix restructures keepOrRevertFileModifications so the source mod_file_path is only removed after the replacement is safely staged (or guards/restores on recovery failure), with subtle symlink-vs-regular-file ordering. This is a behavioral refactor, not a localized guard, and getting it wrong risks data loss or a broken build, so it was skipped. The existing catch(std::runtime_error&) does already catch filesystem_error since it derives from system_error/runtime_error.
- **F134** `src/core/log.cpp:57-67` [Functionality] — Log file is reopened and flushed on every single message
  - **Fix:** Keep a single std::ofstream open for the lifetime of the log file (reopened on init), or batch flushes, to avoid per-message open/close overhead.
  - _Agent note:_ Keeping a single ofstream open for the file lifetime requires a stateful refactor (static stream, init-time open, and coordination with init()'s file-renaming/rotation logic plus threading concerns). Not a localized change; skipped to avoid risking the build.
- **F144** `src/core/moddedapplication.cpp:1545` [Functionality] — Failing deploy hook is logged but never surfaced or aborts deployment
  - **Fix:** Surface a non-zero pre-deploy/pre-undeploy hook result to the UI (or make it configurable whether a failing pre-hook aborts), rather than silently continuing.
  - _Agent note:_ continue-on-hook-failure is intentional documented policy; surfacing to UI is a design change
- **F145** `src/core/moddedapplication.cpp:1579` [Functionality] — addBackup/removeBackup/overwriteBackup do not persist via updateSettings
  - **Fix:** Confirm whether BackupManager persists independently; if not, call updateSettings(true) after these mutations for consistency with the other setters.
  - _Agent note:_ persistence question for BackupManager is a design decision, not a localized fix
- **F146** `src/core/moddedapplication.cpp:1969` [Functionality] — deleteAllData uses throwing filesystem calls and removes download dir without error handling
  - **Fix:** Use the std::error_code overloads, accumulate failures, log them, and continue so a single unremovable file does not abort the whole teardown.
  - _Agent note:_ needs error_code overloads and failure accumulation across teardown, a refactor
- **F148** `src/core/moddedapplication.cpp:2440-2442` [Functionality] — importProfile indexes saved deployers by name; duplicate deployer names silently drop one load order
  - **Fix:** Match saved deployer data by (name, type) and/or by index, and warn when multiple saved entries collide or when a deployer cannot be uniquely matched.
  - _Agent note:_ matching saved deployers by (name,type)/index with collision warnings is a non-trivial change
- **F158** `src/core/pathutils.cpp:93-96` [Functionality] — normalizePath only converts backslashes; does not normalize '..' or duplicate separators
  - **Fix:** Either rename to reflect that it only swaps separators, or extend it to lexically normalize and reject leading '..' segments, and document that it is not a security boundary.
  - _Agent note:_ normalizePath is a public API documented to only swap backslashes for forward slashes; extending it to lexically normalize and reject leading '..' segments would change behavior for all callers (moveFilesToDirectory, renameFiles, etc.) in risky ways, and renaming the function would change the public API. This needs a design decision, so per the SKIP-when-in-doubt rule it is left unchanged.
- **F159** `src/core/plugindeployer.cpp:410-427` [Functionality] — loadPlugins throws on missing plugin file with no caller-side handling guarantee
  - **Fix:** Handle the open failure gracefully in the construction path (treat as empty plugin list) rather than throwing during object construction, or document the throw contract on the constructor.
  - _Agent note:_ loadPlugins() throwing on a missing plugin file is a behavior/contract decision affecting the construction path and existing callers (e.g. restoreUndeployBackupIfExists and constructor flow). Changing it to silently treat the file as empty could mask real errors and alter caller expectations. This needs a design decision, not a localized guard, so skipped per the when-in-doubt rule.
- **F163** `src/core/remote/ommrepository.cpp:98-107` [Functionality] — listPackages re-fetches the network descriptor on every call after a failed connect
  - **Fix:** Cache the connected repository/packages (or move the fetch off the UI thread) and avoid reconstructing OmmRepository plus re-fetching for every selection change; reuse the already-parsed packages_.
- **F164** `src/core/remote/ommrepository.cpp:138-177` [Functionality] — compareVersions can misorder versions via long overflow and mixed numeric/lexical components
  - **Fix:** Compare numeric components as big integers or by (length-then-lexical) for all-digit strings, and define a single consistent ordering for mixed numeric/non-numeric parts rather than switching strategy per component.
  - _Agent note:_ Fixing compareVersions properly requires redefining the ordering strategy (big-integer or length-then-lexical comparison and a single consistent rule for mixed numeric/non-numeric components), which is an algorithm redesign and ordering-semantics design decision rather than a localized guard. Skipped to avoid changing comparison behavior and risking subtle regressions.
- **F165** `src/core/reversedeployer.cpp:464` [Functionality] — enableSeparateDirs mishandles managed-files filename comparison and nested temp path join
  - **Fix:** Compare only filenames against the set of internal bookkeeping files, and join using the relative temp directory name (not the absolute temp_path) so the destination stays under source_path_.
  - _Agent note:_ Skipped: requires design judgment. The line 464 join source_path_ / temp_path / relative_path already resolves correctly because temp_path is absolute (operator/ discards source_path_), so the destination already stays under source_path_; changing it risks a regression. The filename-vs-fullpath bookkeeping comparison change also needs deciding which 'internal bookkeeping files' set to use, which is a behavior change beyond a localized fix.
- **F169** `src/core/tw3mergeutil.cpp:260-275` [Functionality] — findOrCreateContext hardcodes the "context" attribute name, which the code itself flags as unverified
  - **Fix:** Resolve the correct identifying attribute against a real input.xml before relying on this in production; until then, surface a clear 'experimental/unvalidated format' warning in the merge result (the caller does add an experimental note, but the specific risk of created-from-scratch contexts being ignored is not communicated).
  - _Agent note:_ Not a localized code fix. The recommendation is to resolve the correct identifying attribute against a real input.xml and/or surface a specific 'experimental/unvalidated format' risk warning in the merge result. The attribute name cannot be verified here (no game install), and changing user-facing MergeResult messaging is a design/communication decision, not a surgical guard. Left as-is.
- **F170** `src/core/tw3mergeutil.cpp:325-346` [Functionality] — findFragment only searches depth 0 and 1, missing deeper or game-mirrored mod layouts
  - **Fix:** Either perform a bounded recursive search for the fragment file or, when no fragment is found for a source whose directory clearly contains config-like files, log a warning so the silent no-op is diagnosable. At minimum document the depth limit in the user-facing merge result.
  - _Agent note:_ Not a localized code fix. The recommendation is to add a bounded recursive fragment search, emit a diagnostic warning on silent no-ops, or document the depth limit in the user-facing merge result. These are feature/design changes (recursion strategy, logging policy, or message wording) rather than a surgical edit, and risk altering behavior/idempotency. Left as-is.
- **F174** `src/ui/addappdialog.cpp:152-158` [Functionality] — iconIsValid loads the full icon via QIcon just to validate it
  - **Fix:** Cheaply pre-check the path is an existing regular file with an image extension before constructing QIcon, and consider validating off the UI thread for large files.
- **F179** `src/ui/applicationmanager.cpp:253-261` [Functionality] — Auto-update-check flag/interval read without synchronization across threads
  - **Fix:** Make the getters marshal/read via the owning thread, use std::atomic for the flag/interval, or document that the values are only eventually-consistent snapshots.
  - _Agent note:_ setter already marshals via invokeMethod; getter sync needs atomics/doc decision
- **F180** `src/ui/applicationmanager.cpp:544-558` [Functionality] — deployMods emits 'Mods deployed' success even when deployment was skipped or errored
  - **Fix:** Only emit the success message on the successful path; emit a neutral completedOperations() (or no message) when validation fails or an error was already surfaced.
  - _Agent note:_ only-on-success emit needs restructuring success tracking; behavioral, when in doubt skip
- **F181** `src/ui/applicationmanager.cpp:1194-1198` [Functionality] — extractArchive emits extractionComplete even when extraction failed
  - **Fix:** Only emit extractionComplete when extraction succeeded, or document/guarantee that all consumers check info.last_action_was_successful.
  - _Agent note:_ info passed by value; success state not reliably available here, risky
- **F192** `src/ui/ipcserver.cpp:38` [Functionality] — processData has no message framing; partial reads silently drop requests
  - **Fix:** Buffer received bytes per-socket and use a length prefix or newline delimiter to reassemble complete messages before emitting receivedMessage.
- **F193** `src/ui/mainwindow.cpp:1635-1655` [Functionality] — runConcurrent logs full command and runs a member function touching shared state on the thread pool
  - **Fix:** Consider not logging the raw command (or redacting obvious secret-looking args). Capture is_a_flatpak_ by value into the lambda rather than relying on the member access on the worker thread, to keep runCommand thread-safe as it evolves.
  - _Agent note:_ Log redaction and by-value capture are design changes requiring signature edits; left as-is.
- **F202** `src/ui/nexusnewsdialog.cpp:48-67` [Functionality] — News feed fetch uses a detached std::thread with no per-request cancellation or timeout
  - **Fix:** Set an explicit cpr timeout, and consider QtConcurrent + QFutureWatcher for consistency and easier lifetime management.
- **F205** `tests/test_moddedapplication.cpp:40-141` [Functionality] — No test covers deploy hooks (std::system) or pre/post-deploy command execution
  - **Fix:** Add a test that sets pre/post deploy hooks to a harmless command (e.g. writing a sentinel file) and asserts it ran and that a failing hook does not abort deployment, documenting that hook strings are intentionally trusted/verbatim.
  - _Agent note:_ Adding a new deploy-hook test requires authoring a non-trivial test with unverified API signatures; not a localized fix and risks compilation, so skipped.
- **F090** `../limo-deps/cpr/CMakeLists.txt:310-311` [Security] — Bundled cpr 1.15.0 pins curl 8.13.0, which is affected by known CVEs (CVE-2025-5399, CVE-2026-3805)
  - **Fix:** Bump the bundled cpr to a release that FetchContent-pins curl >= 8.19.0, or build with CPR_USE_SYSTEM_CURL=ON against a patched distro curl (>= 8.19.0). Keep CPR_ENABLE_CURL_HTTP_ONLY=ON so the WS/SMB attack surface stays disabled.
- **F091** `.github/workflows/ci.yml:24-39` [Security] — GitHub Actions pinned to mutable major-version tags instead of commit SHAs
  - **Fix:** Pin every `uses:` to a full commit SHA (optionally with a version comment), and enable Dependabot for github-actions to keep the SHAs updated.
- **F092** `.github/workflows/ci.yml:47-52` [Security] — cpr built from a mutable upstream tag rather than a pinned commit SHA
  - **Fix:** Pin to an immutable commit SHA (clone then `git checkout <sha>` and verify, or use a submodule/FetchContent with GIT_TAG set to the full 40-char SHA). Document/verify the expected commit for the 1.14.2 release.
- **F098** `src/core/tool.cpp:253-254, 261-262` [Security] — arguments_ and protontricks_arguments_ are concatenated into the shell command unescaped while the executable path is escaped
  - **Fix:** Document clearly (in tool.h and the edit-tool UI) that these fields are passed to the shell verbatim and must only ever contain trusted, user-authored content; never populate them from mod- or network-derived data, and warn when importing tool configs from untrusted sources.
  - _Agent note:_ The recommended fix is documentation-only and lives in other files (tool.h and the edit-tool UI), which I am not permitted to edit. No localized change is possible within tool.cpp, and the underlying unescaped-argument behavior overlaps with F005 which was skipped as a risky refactor.
- **F107** `src/ui/repositoriesdialog.cpp:69-72, 88-90, 147-150` [Security] — Repository passwords persisted to disk as reversible base64 'obfuscation'
  - **Fix:** Store secrets in the platform secret service (libsecret/KWallet via QtKeychain) instead of base64 in a world-readable JSON file; at minimum restrict the file permissions to 0600 and make clear the password is recoverable.
- **F108** `src/ui/repositoriesdialog.cpp:88-90` [Security] — OpenMW-repo password persisted to disk as reversible base64, not encrypted
  - **Fix:** Encrypt the repo password with the existing cryptography::encrypt (installation-key AES-256-GCM) used for the Nexus API key, instead of base64, or store it in a platform secret store (e.g. via libsecret/QtKeychain). At minimum tighten the file permissions and make the weak-obfuscation warning unmissable.
- **F208** `src/ui/applicationmanager.cpp:918-926` [UI] — Read-only getters emit a user-facing error dialog on invalid app_id
  - **Fix:** Pass show_error=false in these read-only getters: appIndexIsValid(app_id, false), matching getModInfo/getAppInfo.
  - _Agent note:_ lines reference setters changeModVersion/setModNote, not read-only getters; description mismatch
